name: novagator_generator
description: Novagator generator is a code generator for the novagator package.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  analyzer: ^0.39.8
  build: ^1.3.0
  code_builder: ^3.3.0
  source_gen: ^0.9.5

dev_dependencies:
  build_runner: ^1.10.0

dependency_overrides:
  novagator:
    path: ../novagator